# ChatGPT History Jump 中文说明

`ChatGPT History Jump` 是一个面向 ChatGPT 网页端的浏览器扩展，用来在会话页面右侧生成“历史问题目录”，帮助你快速定位自己在当前对话中提过的问题。

## 功能介绍

- 自动扫描当前会话中的用户提问
- 在页面右侧生成可搜索的问题目录
- 点击目录项后平滑跳转到对应提问位置
- 自动高亮当前视口附近的问题
- 支持记住每个会话的目录折叠状态
- 支持识别带图片的问题，并显示“含图”标记
- 支持长问题折叠显示，并通过 `∨` / `∧` 按钮展开或收起

## 适用页面

当前扩展会注入到以下域名：

- `https://chatgpt.com/*`
- `https://chat.openai.com/*`

## 项目结构

- [manifest.json](/mnt/c/Users/admin/OneDrive/Documents/chatgpt-history-jump/manifest.json)：Chrome Extension Manifest V3 配置
- [content.js](/mnt/c/Users/admin/OneDrive/Documents/chatgpt-history-jump/content.js)：核心逻辑，负责扫描消息、生成目录、跳转和状态同步
- [styles.css](/mnt/c/Users/admin/OneDrive/Documents/chatgpt-history-jump/styles.css)：侧边栏和交互样式

## 安装方式

这是一个本地加载的浏览器扩展项目，当前可在 Chrome 和 Edge 中手动安装。

### Chrome 安装步骤

1. 打开 Chrome 浏览器，进入 `chrome://extensions/`
2. 打开右上角“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择当前项目目录
5. 打开 ChatGPT 会话页面，刷新后即可看到右侧目录面板

### Edge 安装步骤

1. 打开 Edge 浏览器，进入 `edge://extensions/`
2. 打开左下角“开发人员模式”
3. 点击“加载解压缩的扩展”
4. 选择当前项目目录
5. 打开 ChatGPT 会话页面，刷新后即可看到右侧目录面板

## 使用说明

### 1. 查看问题目录

进入任意 ChatGPT 会话后，扩展会自动扫描当前页面中的用户消息，并在右侧显示问题列表。

### 2. 搜索问题

在侧边栏顶部的搜索框中输入关键词，可以快速过滤当前会话中的问题。

### 3. 快速跳转

点击任意目录项，会平滑滚动到页面中对应的问题位置，并闪烁提示目标区域。

### 4. 查看带图问题

如果某条用户问题中包含图片，目录项会显示：

- `含图`
- `含N图`

这样可以更快识别带附件或截图的提问。

### 5. 展开长问题

如果问题内容较长，目录里默认只展示精简内容。点击右侧的 `∨` 按钮可以展开完整文本，再次点击可收起。

### 6. 折叠目录

点击目录左侧的小按钮可以折叠或展开整个侧边栏。扩展会按当前会话地址保存折叠状态。

## 实现原理

扩展当前基于 `content script` 工作，主要逻辑如下：

- 监听页面中的用户消息节点
- 提取用户问题文本
- 判断问题是否带图片
- 建立问题列表与页面节点的映射关系
- 通过 `MutationObserver` 响应页面内容变化
- 通过 `IntersectionObserver` 同步当前高亮项
- 通过 `chrome.storage.local` 保存折叠状态

## 当前限制

- 依赖 ChatGPT 当前页面 DOM 结构，若官方页面结构变动，识别逻辑可能需要调整
- 图片识别基于消息节点中的图片元素判断，不是对上传文件语义的深度理解
- 当前只处理“当前会话页面”的问题目录，不跨会话聚合

## 后续可扩展方向

- 支持问题时间戳或轮次信息
- 支持导出当前会话问题目录
- 支持按“纯文本问题 / 带图问题”筛选
- 支持显示图片缩略标记
- 支持更贴近 ChatGPT 原生风格的主题切换

## 版本说明

当前版本：`0.2.24`

如果你后面继续迭代这个项目，建议同步更新这份文档中的功能截图、版本号和已知限制。
    
